# Instrument name corrections
#
# Maps the preferred (correct) name to the current (wrong) TERN vocab label.
# Use this when the vocab database has an incorrectly designated entry and you
# need configs to use the correct name in the meantime.
#
# Remove an entry here once the vocab database has been corrected — the
# preferred name will then be found directly in the vocab with no code change.

corrections:
  Campbell Scientific CR1000X: Campbell CR1000X
